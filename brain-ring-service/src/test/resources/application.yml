spring:
  jpa:
    database: H2
    show-sql: true
  datasource:
    platform: h2
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  liquibase:
    change-log: classpath:migrations/changelog.xml